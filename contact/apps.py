from django.apps import AppConfig


class ContactConfig(AppConfig):
    """
    AppConfig class for the Contact app.
    """

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'contact'
